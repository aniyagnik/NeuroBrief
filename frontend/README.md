# Frontend

React UI for NeuroBrief. See the [root README](../README.md) for setup, development, and deployment.

```bash
# From repo root
npm run dev:ui      # dev server :3000 (proxies API to :5000)
npm run build       # output → frontend/build/
```
